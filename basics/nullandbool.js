//getting this temp from a website api
// var temp = 0;
var temp
//*we will get undefined in output

//now performing some calculation on temp
// temp = 2;


// console.log('the temperature is:' + temp);
console.log(`The Temperature is: ${temp}`);

//next we will be learning conditional programing so we need to k9 abt boolean values

//Boolean - true/false

let grade = true;
// console.log(grade);

let actualMarks = 8;

let myGrade = ( actualMarks < 10 );
let myGrade1 = ( actualMarks < 8 );
//**all logical operations we can use <, >, <=, >=, ==, ===, !=, && and ||

console.log(myGrade);
console.log(myGrade1);
